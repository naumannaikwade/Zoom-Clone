# XZoom landing page

Minimal public page for the features currently available in XZoom.

1. Copy `.env.example` to `.env.local` and change `VITE_APP_URL` when needed.
2. Run `npm install`.
3. Run `npm start` for local development.

Use `npm test`, `npm run lint`, and `npm run build` before deployment.
